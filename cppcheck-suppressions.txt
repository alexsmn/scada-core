// Cppcheck suppressions file
// Add suppressions in the format: [error id]:[filename]:[line]
// Or just [error id] to suppress globally
// See: https://cppcheck.sourceforge.io/manual.html#suppressions

// Boost macros not recognized by cppcheck
unknownMacro

// cppcheck branch analysis limit (informational, not a real error)
normalCheckLevelMaxBranches

// Paths are written with forward slashes only. cppcheck normalizes '\' to '/'
// before matching, on every platform, so a backslash twin of an entry is not a
// Windows variant of it -- it is the same suppression twice, and a duplicate is
// a hard error ("suppression '...' already exists") that aborts the whole run
// before a single file is checked.

// False positive: browse_name is moved into aggregate init, not accessed after move
accessMoved:*scada/node.cpp

passedByValue:*scada/node.cpp

// GoogleTest macros inside namespaces
syntaxError

// Protobuf generated code version checks
preprocessorErrorDirective

// Exclude external/third-party directories
*:*/third_party/*
*:*/vcpkg/*

// Exclude build directory (generated files)
*:*/build/*
